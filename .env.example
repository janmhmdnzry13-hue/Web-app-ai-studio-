# Server-Side Secrets (Never expose with VITE_ prefix)
# GEMINI_API_KEY: Required for Gemini AI API calls. Managed via Settings / Secrets in AI Studio.
GEMINI_API_KEY=

# Application URL (Injected at runtime by hosting container)
APP_URL=
